## A

*   **air gap** (изолирана среда, въздушна междина)
    *   EN: The military control system is protected by an *air gap* to prevent remote hacking.
    *   BG: Военната система за контрол е защитена чрез *изолирана среда*, за да се предотврати отдалечено хакване.

*   **(an) assertion** (твърдение, проверка)
    *   EN: Add an *assertion* to confirm the variable's state.
    *   BG: Добавете *твърдение*, за да подсигурите състоянието на променливата.

*   **attenuation** (затихване)
    *   EN: Using a high-quality coaxial cable helps to minimize signal *attenuation*.
    *   BG: Използването на висококачествен коаксиален кабел помага за намаляване на *затихването* на сигнала.

## B

*   **(a) bootloader** (начален зареждащ софтуер, заредител)
    *   EN: The *bootloader* must verify the firmware signature before executing the main application.
    *   BG: *Началният зареждащ софтуер* трябва да провери подписа на фърмуера, преди да изпълни основното приложение.

*   **(a) buffer overflow** (препълване на буфера)
    *   EN: The security update fixes a critical *buffer overflow* vulnerability in the network stack.
    *   BG: Актуализацията за сигурност отстранява критична уязвимост от *препълване на буфера* в мрежовия стек.

## C

*   **cross-compilation** (кроскомпилация, междуплатформено компилиране)
    *   EN: We set up a toolchain for *cross-compilation* to build the binary for the ARM target.
    *   BG: Настроихме инструментариум за *кроскомпилация*, за да изградим двоичния файл за целевата ARM архитектура.

*   **concurrency** (конкурентност, паралелизъм)
    *   EN: Proper synchronization is essential to manage *concurrency* without causing data races.
    *   BG: Правилната синхронизация е от съществено значение за управление на *конкурентността* без поява на състезание за данни.

## D

*   **(a) daemon** (демон, фонов процес)
    *   EN: The logging *daemon* runs continuously in the background to record system events.
    *   BG: *Демонът* за логване работи непрекъснато във фонов режим, за да записва системните събития.

*   **(a) deadlock** (взаимно блокиране, безизходно положение)
    *   EN: The program entered a *deadlock* because thread A and thread B were waiting for each other's locks.
    *   BG: Програмата влезе във *взаимно блокиране*, тъй като нишка А и нишка Б чакаха заключващите механизми една на друга.

## E

*   **(an) exploit** (експлойт, злоупотреба)
    *   EN: The researcher published a proof-of-concept *exploit* to demonstrate the vulnerability.
    *   BG: Изследователят публикува концептуален *експлойт*, за да демонстрира уязвимостта.

*   **entropy** (ентропия)
    *   EN: Linux gathers *entropy* from hardware events to generate cryptographically secure random numbers.
    *   BG: Linux събира *ентропия* от хардуерни събития, за да генерира криптографски сигурни случайни числа.

## F

*   **firmware** (фърмуер, вграден софтуер)
    *   EN: Do not power off the device while the *firmware* update is in progress.
    *   BG: Не изключвайте устройството, докато се извършва актуализацията на *фърмуера*.

*   **fuzzing** (фъзинг, тестване с произволни данни)
    *   EN: Automated *fuzzing* helped us discover several unhandled exceptions in the parser.
    *   BG: Автоматизираният *фъзинг* ни помогна да открием няколко необработени изключения в парсера.

## G

*   **GPIO (General-purpose input/output)** (входно-изходни портове с общо предназначение)
    *   EN: You can configure this *GPIO* pin as an input to read the button state.
    *   BG: Можете да конфигурирате този *GPIO* щифт като вход, за да прочетете състоянието на бутона.

*   **graceful degradation** (плавно влошаване на работата)
    *   EN: In case of sensor failure, the system relies on *graceful degradation* to maintain basic control.
    *   BG: В случай на повреда на сензора, системата разчита на *плавно влошаване на работата*, за да запази основен контрол.

## H

*   **hard real-time** (строго реално време)
    *   EN: An airbag deployment system requires a *hard real-time* response to ensure safety.
    *   BG: Системата за задействане на въздушни възглавници изисква реакция в *строго реално време*, за да гарантира безопасност.

*   **(a) honeypot** (примамка, капан)
    *   EN: The security team deployed a *honeypot* to capture and analyze malicious traffic.
    *   BG: Екипът по сигурност разположи *примамка*, за да прихване и анализира зловредния трафик.

## I

*   **(an) inode** (индексен възел)
    *   EN: Each file on a POSIX filesystem is associated with a unique *inode* number.
    *   BG: Всеки файл в POSIX файлова система е свързан с уникален номер на *индексен възел*.

*   **(an) interrupt service routine (ISR)** (подпрограма за обслужване на прекъсване)
    *   EN: Keep the *interrupt service routine* as short as possible to avoid delaying other events.
    *   BG: Дръжте *подпрограмата за обслужване на прекъсване* възможно най-кратка, за да избегнете забавянето на други събития.

## J

*   **(a) jailbreak** (бягство от затвора, модифициране)
    *   EN: The *jailbreak* allowed users to install custom software on the locked smart TV.
    *   BG: *Модифицирането* позволи на потребителите да инсталират потребителски софтуер на заключения смарт телевизор.

*   **jitter** (трептене, отклонение)
    *   EN: High network *jitter* can cause noticeable delays in real-time audio transmission.
    *   BG: Голямото *трептене* в мрежата може да причини забележими закъснения при предаването на аудио в реално време.

## K

*   **(a) kernel panic** (срив на ядрото)
    *   EN: A corrupted memory driver caused a *kernel panic* during system boot.
    *   BG: Повреден драйвер за паметта предизвика *срив на ядрото* по време на стартиране на системата.

*   **(a) keylogger** (клавиатурен шпионин)
    *   EN: The antivirus software detected a hidden *keylogger* capturing user credentials.
    *   BG: Антивирусният софтуер откри скрит *клавиатурен шпионин*, който записва потребителски данни за вход.

## L

*   **least privilege** (минимални привилегии)
    *   EN: Following the principle of *least privilege*, the application runs without root permissions.
    *   BG: Следвайки принципа за *минимални привилегии*, приложението се изпълнява без администраторски права.

*   **(a) linker script** (свързващ скрипт)
    *   EN: Modify the *linker script* to allocate more space for the application stack in RAM.
    *   BG: Променете *свързващия скрипт*, за да заделите повече пространство за стека на приложението в RAM паметта.

## M

*   **malware** (зловреден софтуер)
    *   EN: The firewalls are configured to block and isolate known *malware* signatures.
    *   BG: Защитните стени са конфигурирани да блокират и изолират сигнатури на познат *зловреден софтуер*.

*   **memory-mapped I/O** (вход-изход с картографиране в паметта)
    *   EN: The microchip uses *memory-mapped I/O* to control the LED indicators directly.
    *   BG: Микрочипът използва *вход-изход с картографиране в паметта*, за да управлява директно LED индикаторите.

## N

*   **non-volatile memory (NVM)** (енергонезависима памет)
    *   EN: Configuration settings are saved in *non-volatile memory* so they are not lost after a reboot.
    *   BG: Конфигурационните настройки се записват в *енергонезависима памет*, за да не се загубят след рестартиране.

*   **(a) network socket** (мрежово гнездо)
    *   EN: The application opens a *network socket* to listen for incoming client connections.
    *   BG: Приложението отваря *мрежово гнездо*, за да слуша за входящи връзки от клиенти.

## O

*   **obfuscation** (замаскиране, замъгляване)
    *   EN: Code *obfuscation* makes it difficult for competitors to reverse engineer our proprietary logic.
    *   BG: *Замаскирането* на кода затруднява конкурентите при извършването на обратен инженеринг на нашата патентована логика.

*   **out-of-band** (извънчестотен, страничен)
    *   EN: We established an *out-of-band* management connection via a dedicated serial port.
    *   BG: Установихме *странична* връзка за управление чрез специализиран сериен порт.

## P

*   **polling** (периодично запитване, допитване)
    *   EN: Using interrupts is often more efficient than constant register *polling*.
    *   BG: Използването на прекъсвания често е по-ефективно от постоянното *периодично запитване* на регистрите.

*   **phishing** (фишинг, мрежово въдичарство)
    *   EN: Employees received training to help them identify suspicious *phishing* emails.
    *   BG: Служителите преминаха обучение, което да им помогне да разпознават подозрителни *фишинг* имейли.

## Q

*   **(a) quota** (квота, ограничение)
    *   EN: The administrator set a strict disk *quota* for each user in the shared directory.
    *   BG: Администраторът зададе строга дискова *квота* за всеки потребител в споделената директория.

## R

*   **(a) rootkit** (руткит)
    *   EN: A sophisticated *rootkit* can modify system calls to hide its files from the operating system.
    *   BG: Сложен *руткит* може да модифицира системните повиквания, за да скрие файловете си от операционната система.

*   **(a) race condition** (състезание, конкуренция за ресурси)
    *   EN: Without a mutex, concurrent write operations can lead to a dangerous *race condition*.
    *   BG: Без мутекс, едновременните операции по запис могат да доведат до опасно *състезание* за ресурси.

## S

*   **(a) semaphore** (семафор)
    *   EN: Use a *semaphore* to limit the number of threads accessing the shared resource simultaneously.
    *   BG: Използвайте *семафор*, за да ограничите броя на нишките, които имат достъп до споделения ресурс едновременно.

*   **spoofing** (замаскиране, фалшифициране)
    *   EN: The router was configured to detect and drop packets associated with IP *spoofing*.
    *   BG: Рутерът беше конфигуриран да открива и отхвърля пакети, свързани с *фалшифициране* на IP адреси.

## T

*   **(a) thread** (нишка, нишка на изпълнение)
    *   EN: The background *thread* handles data processing without freezing the user interface.
    *   BG: Фоновата *нишка* обработва данните, без да замразява потребителския интерфейс.

*   **threat modeling** (моделиране на заплахи)
    *   EN: Performing *threat modeling* early in the design phase helps secure the architecture.
    *   BG: Извършването на *моделиране на заплахи* в ранен етап от фазата на проектиране помага за защитата на архитектурата.

## U

*   **user space** (потребителско пространство)
    *   EN: Standard applications run in *user space* to prevent them from crashing the kernel.
    *   BG: Стандартните приложения се изпълняват в *потребителското пространство*, за да се предотврати срив на ядрото от тяхна страна.

*   **(an) underrun** (недостиг на данни)
    *   EN: Slow memory read speeds caused a buffer *underrun* during audio playback.
    *   BG: Бавните скорости на четене от паметта предизвикаха *недостиг на данни* в буфера по време на възпроизвеждане на аудио.

## V

*   **(a) vector table** (векторна таблица)
    *   EN: During startup, the CPU reads the *vector table* to locate the reset handler address.
    *   BG: По време на стартиране процесорът чете *векторната таблица*, за да намери адреса на обработчика при нулиране.

*   **(a) vulnerability** (уязвимост)
    *   EN: The team discovered a critical *vulnerability* in the network parser library.
    *   BG: Екипът откри критична *уязвимост* в библиотеката за мрежов анализ.

## W

*   **(a) watchdog timer** (пазач, стражеви таймер)
    *   EN: If the main loop freezes, the *watchdog timer* will reset the microchip.
    *   BG: Ако основният цикъл забие, *стражевият таймер* ще рестартира микрочипа.

## X

*   **XSS (Cross-Site Scripting)** (междусайтово изпълнение на скриптове)
    *   EN: Input sanitization is necessary to protect the web interface from *XSS* attacks.
    *   BG: Санирането на входните данни е необходимо за защита на уеб интерфейса от *XSS* атаки.

## Y

*   **(the) yellow team** (жълт екип)
    *   EN: The *yellow team* focused on writing clean, secure code to resist security threats.
    *   BG: *Жълтият екип* се фокусира върху писането на чист и сигурен код, който да издържа на заплахи за сигурността.

## Z

*   **(a) zombie process** (процес-зомби)
    *   EN: A *zombie process* does not consume CPU resources, but it still occupies an entry in the process table.
    *   BG: *Процесът-зомби* не консумира ресурси на процесора, но все пак заема запис в таблицата с процеси.

*   **(a) zero-day** (уязвимост от нулев ден)
    *   EN: The attackers used a *zero-day* exploit before any security patch was available.
    *   BG: Нападателите използваха експлойт за уязвимост от *нулев ден*, преди да има наличен пач за сигурност.
